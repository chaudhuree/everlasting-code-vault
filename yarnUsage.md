- jetay yarn use korte chai setai

```bash
yarn init -y
```

-then create a file in the folder named yarn.lock

```bash
touch yarn.lock
```

- then to install dependencies

```bash
yarn
```

- to install a dependency

```bash
yarn add <package-name>
```

- to run the project

```bash
yarn start
```

- or,

```bash
  yarn dev
```
